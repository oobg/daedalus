src/components/viewer/Viewer25D.tsx — Updated 2.5D viewer using RoundedBox from @react-three/drei for softened wall geometry, GTAOPass for ambient occlusion, meshStandardMaterial replacing meshLambertMaterial, floor inset/elevation separation, wall base offset, contact shadows via ambient-occlusion module.

src/components/viewer/viewer25dGeometry.ts — Geometry utilities: createInsetPolygon, resolveFloorBaseElevationOffset, resolveFloorPerimeterInset, resolveWallBaseElevationOffset, createWallMeshAssembly, createExteriorWallMeshAssembly, softened corner/segment geometry.

src/features/viewer/wall-shading.ts — getWallShadingConfig() returns soft matte params (roughness:0.85, metalness:0, low specular) for interior/exterior walls.

src/features/viewer/wall-color-palette.ts — Warm architectural tone palette.

src/features/viewer/wood-accent-shading.ts — Warm wood-toned material params (roughness:0.75, warm color).

src/features/viewer/glass-material.ts — getGlassMaterialConfig() with subtle transparency (opacity:0.35) and restrained reflectivity (roughness:0.15, metalness:0.1).

src/features/viewer/ambient-lighting.ts — getAmbientLightingPreset() with soft intensity (0.7) and warm-neutral color (#fff5e6).

src/features/viewer/ambient-occlusion.ts — resolveAmbientOcclusionSettings() configures GTAO with gentle strength (0.4), radius (4), no overshadow.

src/features/viewer/scene-element-material-tags.ts — classifySceneElementMaterialTags() maps walls/floors/windows to material tags.

src/features/viewer/scene-material-assignment.ts — applySceneElementRenderMaterials() dispatches resolved materials to scene elements.

src/features/viewer/furniture-base-elevation.ts — computeFurnitureBaseElevation() applies layer offset above floor.

src/features/viewer/furniture-asset-metadata.ts — validateFurnitureAssetMetadata() approves low-poly, rejects game-prop/realistic-scan assets.

src/features/viewer/viewer-presentation.ts — getViewerPresentationPreset() bundles full diorama lighting/material/geometry config.

src/features/viewer/exterior-wall-mesh.ts — createExteriorWallMeshAssembly() softened exterior wall geometry.

src/features/viewer/index.ts — now also exports RendererPort and RenderSceneData types.

Pre-existing failures (unrelated to viewer): 5 editor-state tests fail and 7 TS errors in import-uploaded-project/validate-uploaded-project-schema tests, all predating this work.